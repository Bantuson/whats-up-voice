---
phase: 05-tests-frontend-demo
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - .planning/phases/05-tests-frontend-demo/DEMO-CHECKLIST.md
autonomous: false
requirements: [TEST-01, FE-01, FE-02, FE-03, FE-04, FE-05, FE-06, FE-07, FE-08, MEM-01, MEM-02, MEM-03]

must_haves:
  truths:
    - "`bun test` reports 85+ passing, 0 failing — confirmed in final run"
    - "All 11 suite describe-block names present in bun test output"
    - "Frontend loads at http://localhost:5173 with all 7 pages functional"
    - "Demo environment has valid Meta system user token (generated within 1 hour of demo)"
    - "Real WhatsApp message received → heartbeat feed updates within 2 seconds in dashboard"
    - "Full demo script completes without error: receive → interrupt → TTS → compose → approve → send → briefing"
  artifacts:
    - path: ".planning/phases/05-tests-frontend-demo/DEMO-CHECKLIST.md"
      provides: "Pre-demo verification checklist with pass/fail columns"
      contains: "Meta system user token"
    - path: "frontend/"
      provides: "Production-ready dashboard"
  key_links:
    - from: "DEMO-CHECKLIST.md"
      to: "live demo environment"
      via: "manual verification steps"
      pattern: "checklist"
---

<objective>
Verify the full system is demo-ready: confirm 85+ tests pass, confirm frontend is functional end-to-end, verify all external services are active, and produce a pre-demo checklist.

Purpose: This is the final validation gate before the hackathon demo. Every subsystem must be green-lit individually before attempting the end-to-end run. The demo checklist ensures nothing is forgotten under demo pressure.

Output: DEMO-CHECKLIST.md with concrete pass/fail steps; confirmed test suite count; confirmed frontend operational; ElevenLabs voice IDs benchmarked and committed; environment verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final test suite verification — confirm 85+ passing, 0 failing</name>
  <read_first>
    - .planning/phases/05-tests-frontend-demo/05-VALIDATION.md (11 required suite names)
    - tests/ directory listing (all test files)
  </read_first>
  <files>.planning/phases/05-tests-frontend-demo/DEMO-CHECKLIST.md</files>
  <action>
    Run the full test suite and audit the output:

    Step 1 — Run full suite:
    ```bash
    bun test 2>&1 | tee /tmp/test-output.txt
    ```

    Step 2 — Count passing and failing:
    ```bash
    tail -5 /tmp/test-output.txt
    ```

    Step 3 — Verify all 11 suite names are present in output:
    ```bash
    grep -i "quiet hours\|E.164 normalisation\|HMAC verification\|heartbeat gate\|intent classification\|session state machine\|cron validation\|Message log helpers\|morning briefing\|contact save flow\|WhatsApp payload parsing" /tmp/test-output.txt
    ```

    Step 4 — If any tests are still failing, diagnose and fix before proceeding. Do not advance to demo prep with failing tests.

    Step 5 — Create DEMO-CHECKLIST.md:
    ```markdown
    # VoiceApp Demo Checklist

    **Phase:** Demo day — run this checklist in order, top to bottom.
    **Time budget:** 30 minutes before demo start.

    ---

    ## 1. Test Suite (run first)

    | Check | Command | Expected | Status |
    |-------|---------|----------|--------|
    | All tests pass | `bun test` | 85+ passing, 0 failing | [ ] |
    | 11 suite names in output | grep in bun test output | All 11 present | [ ] |

    ## 2. Environment Variables

    | Variable | Where to find | Status |
    |----------|---------------|--------|
    | WHATSAPP_APP_SECRET | Meta Business Manager → App → Basic Settings | [ ] |
    | WHATSAPP_ACCESS_TOKEN | Meta Business Manager → System User → Generate Token (expires!) | [ ] |
    | WHATSAPP_PHONE_NUMBER_ID | Meta → WhatsApp → Getting Started | [ ] |
    | WHATSAPP_VERIFY_TOKEN | Your .env file | [ ] |
    | ANTHROPIC_API_KEY | console.anthropic.com | [ ] |
    | OPENAI_API_KEY | platform.openai.com | [ ] |
    | ELEVENLABS_API_KEY | elevenlabs.io → Profile | [ ] |
    | REDIS_URL | Upstash or Railway dashboard | [ ] |
    | SUPABASE_URL | Supabase project settings | [ ] |
    | SUPABASE_SERVICE_ROLE_KEY | Supabase project settings → API | [ ] |
    | API_BEARER_TOKEN | Your .env file | [ ] |

    ## 3. Meta / WhatsApp

    | Check | Action | Status |
    |-------|--------|--------|
    | System user token fresh | Generate NEW token within 1 hour of demo start | [ ] |
    | WABA tier | Business Manager → WhatsApp → Overview — confirm 250-message limit NOT reached | [ ] |
    | Webhook active | Meta → WhatsApp → Configuration — webhook URL shows green | [ ] |
    | Test phone registered | Send test message and confirm it arrives | [ ] |

    ## 4. Backend Health

    | Check | Command | Expected | Status |
    |-------|---------|----------|--------|
    | Server starts | `bun run src/server.ts` | No errors | [ ] |
    | Health check | `curl http://localhost:3000/health` | `{"status":"ok"}` | [ ] |
    | Redis connected | Server logs on start | No "ECONNREFUSED" | [ ] |
    | Supabase connected | `bun test tests/schema.test.ts` | 0 failing | [ ] |

    ## 5. Frontend

    | Check | Action | Status |
    |-------|--------|--------|
    | Dev server starts | `cd frontend && bun run dev` | Port 5173, no errors | [ ] |
    | Login page loads | Open http://localhost:5173 | Redirects to /login | [ ] |
    | Enter phone + Connect | Enter +27 number | Redirects to /dashboard | [ ] |
    | Dashboard renders | Check waveform shows | 24 grey bars visible | [ ] |
    | All 7 nav pages load | Click each nav item | No blank pages | [ ] |

    ## 6. ElevenLabs Voice IDs

    | Check | Action | Status |
    |-------|--------|--------|
    | English SA voice selected | elevenlabs.io → Voice Library → filter "South Africa" | Note voice ID | [ ] |
    | ELEVENLABS_VOICE_ID set in .env | Check .env | Correct voice ID | [ ] |
    | Afrikaans voice selected (if needed) | elevenlabs.io → Voice Library → filter "Afrikaans" | Note voice ID | [ ] |
    | TTS test | `POST /api/voice/command` with "hello" | Audio plays via WebSocket | [ ] |

    ## 7. EskomSePush

    | Check | Action | Status |
    |-------|--------|--------|
    | Area ID confirmed | eskomsepush.app → Search your area | Note area ID | [ ] |
    | ESKOMSEPUSH_AREA_ID in .env | Check .env | Valid ID or "johannesburg" | [ ] |
    | Load shedding query | Ask agent "load shedding today" | Response in < 3 seconds | [ ] |

    ## 8. End-to-End Demo Rehearsal

    Run this at least 24 hours before demo day. Run again 1 hour before.

    | Step | Action | Expected | Status |
    |------|--------|----------|--------|
    | 1. Receive WhatsApp | Send message from test phone | Appears in heartbeat feed | [ ] |
    | 2. Heartbeat interrupt | Confirm decision = interrupt | Feed row is green | [ ] |
    | 3. TTS read-aloud | WebSocket client receives audio | Spoken aloud | [ ] |
    | 4. Voice compose | POST /api/voice/command "send message to Naledi, I'll be late" | session = awaiting_approval | [ ] |
    | 5. Approval | POST with "yes" transcript | message_log has direction=out row | [ ] |
    | 6. Morning briefing | Trigger via BullMQ | Load shedding spoken before weather | [ ] |
    | 7. Memory stored | Check memory_store | New row with non-null embedding | [ ] |
    | 8. Memory recalled | Ask about topic from step 4 | Memory snippet in system prompt log | [ ] |

    ---

    **Signed off by:** ___________  **Date/time:** ___________
    ```
  </action>
  <verify>
    <automated>bun test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` output shows 0 failing tests
    - `bun test` total passing count is 85 or higher
    - DEMO-CHECKLIST.md file exists at `.planning/phases/05-tests-frontend-demo/DEMO-CHECKLIST.md`
    - DEMO-CHECKLIST.md contains "Meta system user token" (checklist item for token freshness)
    - DEMO-CHECKLIST.md contains "250-message limit" (WABA tier check)
    - DEMO-CHECKLIST.md contains the 8-step end-to-end demo rehearsal table
  </acceptance_criteria>
  <done>bun test shows 85+ passing, 0 failing; DEMO-CHECKLIST.md created with all 8 demo rehearsal steps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Frontend visual verification + end-to-end demo rehearsal</name>
  <what-built>
    Plan 05-03 built the full Vite + React 18 caregiver dashboard with 7 pages, SSE integration, and terminal aesthetic. Plan 05-01 fixed all test failures. Plan 05-02 added episodic memory. This checkpoint verifies the complete system works together before the demo.
  </what-built>
  <how-to-verify>
    Run through the following steps in order. Report any step that fails.

    **Backend:**
    1. Start the backend: `bun run src/server.ts` — confirm no errors in startup logs
    2. Health check: `curl http://localhost:3000/health` — confirm `{"status":"ok","timestamp":"..."}`

    **Frontend:**
    3. Start frontend: `cd frontend && bun run dev` — confirm "Local: http://localhost:5173/"
    4. Open http://localhost:5173 in browser — should redirect to #/login
    5. Verify Login page:
       - "VoiceApp" title is green (#00FF88)
       - Phone field placeholder is "+27 83 100 0000"
       - "Connect" button is green
       - Background is very dark (#0D0D0D)
    6. Enter "+27831000000" and click Connect — should redirect to #/dashboard
    7. Verify Dashboard:
       - Shows "idle" session phase in muted grey
       - 24 grey static bars visible as waveform
       - "Send Command" input field and button present
    8. Click FEED in nav — verify "No heartbeat events yet. Waiting for WhatsApp messages."
    9. Click CONTACTS in nav — verify page loads (empty state or contact list)
    10. Click ROUTINES in nav — verify page loads
    11. Click LOG in nav — verify three sections (Message History, Heartbeat Audit, Memory Schema Viewer)
    12. Nav sidebar: verify left border turns green on active route

    **Test suite (final):**
    13. Run: `bun test` — confirm output ends with "N pass, 0 fail" where N >= 85
    14. Scroll up in bun test output — confirm "Message log helpers" describe block visible
    15. Confirm "Episodic memory" describe block visible in output

    **External services (if available):**
    16. Send a real WhatsApp message from test phone — confirm heartbeat feed row appears within 2 seconds
    17. If Supabase deployed: run `bun test tests/schema.test.ts` — confirm 0 failing
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Type "issues: [description]" if something failed — Claude will fix and re-request verification.
  </resume-signal>
  <files>.planning/phases/05-tests-frontend-demo/DEMO-CHECKLIST.md</files>
  <action>Human verification checkpoint — execute the how-to-verify steps listed above. No code changes required in this task; all automated work was completed in Tasks 1, 05-01, 05-02, and 05-03.</action>
  <verify><automated>MANUAL — human approval via resume-signal required</automated></verify>
  <done>Human types approved confirming all 17 verification steps pass</done>
</task>

</tasks>

<verification>
System is demo-ready when:
1. `bun test` shows 85+ passing, 0 failing
2. All 11 suite describe-block names present in output
3. Frontend builds and renders all 7 pages without JavaScript errors
4. DEMO-CHECKLIST.md exists with 8-step end-to-end rehearsal table
5. Human has approved visual verification via checkpoint above
</verification>

<success_criteria>
- `bun test` reports 85+ passing, 0 failing (confirmed by human during checkpoint)
- Frontend at http://localhost:5173 renders all 7 pages
- DEMO-CHECKLIST.md committed to .planning/phases/05-tests-frontend-demo/
- Hackathon demo can be run end-to-end: WhatsApp in → heartbeat → TTS → compose → approve → send → morning briefing
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests-frontend-demo/05-04-SUMMARY.md`
</output>
