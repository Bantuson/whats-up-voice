---
phase: 03-agent-intelligence
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - CONTACT-01
  - AGENT-01
  - AGENT-02

must_haves:
  truths:
    - "CONTACT-01 is explicitly assigned to Phase 4 in ROADMAP.md — not listed as Phase 3 or orphaned"
    - "ROADMAP.md Phase 4 requirements list includes CONTACT-01"
    - "ROADMAP.md Phase 3 requirements list does NOT include CONTACT-01"
    - "A rationale note explains why CONTACT-01 belongs in Phase 4 (requires pushInterrupt TTS wiring)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "CONTACT-01 formally assigned to Phase 4 with rationale"
      contains: "CONTACT-01"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 4 requirements list now includes CONTACT-01"
      pattern: "CONTACT-01"
---

<objective>
Formally assign CONTACT-01 to Phase 4 by updating ROADMAP.md and REQUIREMENTS.md.

CONTACT-01 defines: "Unknown number inbound triggers interrupt with spoken phone number
(digit-by-digit format: 'plus 2 7 8 3 1...')". This requirement belongs in Phase 4, not Phase 3,
for the following architectural reason: the interrupt delivery mechanism (`pushInterrupt()`) is a
WebSocket stub in Phase 2/3. Real TTS-driven audio push requires the ElevenLabs TTS module and
WebSocket audio pipeline, both of which are built in Phase 4. The CONTACT-01 interrupt cannot
produce a spoken digit-by-digit reading until Phase 4 provides the TTS layer.

The ROADMAP Phase 2 description already placed CONTACT-01 in the heartbeat gate, but that plan
was deferred. The surface decision gate in Phase 2 has the `pushInterrupt()` call site — Phase 4
will replace the stub with real TTS. CONTACT-01 should be wired at that point.

This plan updates planning documents only. No production code is changed.

Purpose: Eliminate the orphaned requirement gap so verification passes cleanly.
Output: ROADMAP.md and REQUIREMENTS.md with CONTACT-01 formally moved to Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agent-intelligence/03-VERIFICATION.md

<interfaces>
<!-- Relevant ROADMAP sections for context -->

Phase 3 requirements line (currently):
  "Requirements covered: AGENT-01, AGENT-02, ..., CONTACT-01, CONTACT-02, CONTACT-03, CONTACT-04, CONTACT-05"

Phase 4 requirements line (currently):
  "Requirements covered: VOICE-01, VOICE-02, VOICE-03, VOICE-04, VOICE-05, CRON-01, CRON-02, CRON-03, CRON-04"

Phase 4 heartbeat integration point (ROADMAP Phase 4, Plan 2 description):
  "...session state transitions wired throughout; received voice note playback..."
  → CONTACT-01 should be noted here: when pushInterrupt() gains TTS, unknown-number events
    trigger spoken digit-by-digit phone format

REQUIREMENTS.md CONTACT-01 line (currently):
  "- [ ] **CONTACT-01**: Unknown number inbound triggers `interrupt` with spoken phone number (digit-by-digit format: 'plus 2 7 8 3 1...')"
  — currently marked as Phase 3 in the Traceability table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CONTACT-01 from Phase 3 to Phase 4 in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md (read full file — find Phase 3 and Phase 4 Requirements covered lines and Phase 4 plan descriptions)
    - .planning/phases/03-agent-intelligence/03-VERIFICATION.md (Gap 2 description — ORPHANED requirement analysis)
  </read_first>

  <action>
Edit ROADMAP.md. Make three targeted changes:

**Change 1 — Phase 3 "Requirements covered" line:**

Find the line under Phase 3 heading:
```
**Requirements covered:** AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06, AGENT-07, AGENT-08, CONTACT-01, CONTACT-02, CONTACT-03, CONTACT-04, CONTACT-05
```

Remove CONTACT-01 from this list:
```
**Requirements covered:** AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06, AGENT-07, AGENT-08, CONTACT-02, CONTACT-03, CONTACT-04, CONTACT-05
```

**Change 2 — Phase 4 "Requirements covered" line:**

Find the line under Phase 4 heading:
```
**Requirements covered:** VOICE-01, VOICE-02, VOICE-03, VOICE-04, VOICE-05, CRON-01, CRON-02, CRON-03, CRON-04
```

Add CONTACT-01:
```
**Requirements covered:** VOICE-01, VOICE-02, VOICE-03, VOICE-04, VOICE-05, CRON-01, CRON-02, CRON-03, CRON-04, CONTACT-01
```

**Change 3 — Phase 4 Plan 2 description:**

Find the Phase 4, Plan 2 description line that ends with:
```
...session state transitions wired throughout; received voice note playback (fetch from WhatsApp Media URL, stream to device)
```

Append a sentence to the end of that plan's description:
```
; CONTACT-01: when pushInterrupt() delivers TTS audio, unknown-number events trigger spoken digit-by-digit phone reading via formatPhoneForSpeech() — the heartbeat gate already identifies unknown numbers (Phase 2), Phase 4 wires the TTS delivery
```

Do NOT change: any other phase descriptions, success criteria, dependency statements, or plan lists.
  </action>

  <verify>
    <automated>grep -n "CONTACT-01" /c/Users/Bantu/mzansi-agentive/voice-app/.planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CONTACT-01" .planning/ROADMAP.md` returns results in Phase 4 section only (not Phase 3 Requirements covered line)
    - Phase 3 "Requirements covered" line does NOT contain CONTACT-01
    - Phase 4 "Requirements covered" line DOES contain CONTACT-01
    - Phase 4 Plan 2 description mentions CONTACT-01 with rationale
    - `grep -c "CONTACT-01" .planning/ROADMAP.md` returns at least 2 (requirements line + plan description)
  </acceptance_criteria>

  <done>ROADMAP.md assigns CONTACT-01 to Phase 4 with architectural rationale. Phase 3 no longer claims it.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md traceability table for CONTACT-01</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (lines 125-137 — the Traceability table; also find CONTACT-01 checkbox line)
  </read_first>

  <action>
Edit REQUIREMENTS.md. Make two targeted changes:

**Change 1 — CONTACT-01 checkbox line:**

Find:
```
- [ ] **CONTACT-01**: Unknown number inbound triggers `interrupt` with spoken phone number (digit-by-digit format: 'plus 2 7 8 3 1...')
```

Keep the checkbox and text unchanged. Add a trailing note:
```
- [ ] **CONTACT-01**: Unknown number inbound triggers `interrupt` with spoken phone number (digit-by-digit format: 'plus 2 7 8 3 1...') *(Deferred to Phase 4 — requires TTS/pushInterrupt wiring from Phase 4 Plan 2)*
```

**Change 2 — Traceability table:**

Find the row:
```
| CONTACT-01 – CONTACT-05 | Phase 3 | Pending |
```

Split into two rows:
```
| CONTACT-02 – CONTACT-05 | Phase 3 | Pending |
| CONTACT-01              | Phase 4 | Pending |
```

Do NOT change any other rows, the coverage totals, or the v0.2/Out of Scope sections.
  </action>

  <verify>
    <automated>grep -n "CONTACT-01" /c/Users/Bantu/mzansi-agentive/voice-app/.planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "CONTACT-01" .planning/REQUIREMENTS.md` shows Phase 4 in the traceability table (not Phase 3)
    - The CONTACT-01 checkbox line now includes a parenthetical deferral note
    - The traceability table still has CONTACT-02 through CONTACT-05 mapped to Phase 3
    - Total requirement count in REQUIREMENTS.md is unchanged (no requirements added or removed)
    - `grep -c "Phase 4" .planning/REQUIREMENTS.md` is one more than before (new traceability row)
  </acceptance_criteria>

  <done>REQUIREMENTS.md traceability table correctly shows CONTACT-01 in Phase 4. CONTACT-02 through CONTACT-05 remain in Phase 3.</done>
</task>

</tasks>

<verification>
Confirm CONTACT-01 is no longer orphaned:

```bash
grep -n "CONTACT-01" .planning/ROADMAP.md
grep -n "CONTACT-01" .planning/REQUIREMENTS.md
```

Phase 3 ROADMAP line must not mention CONTACT-01.
Phase 4 ROADMAP line must mention CONTACT-01.
REQUIREMENTS.md traceability must show Phase 4 for CONTACT-01.

Cross-check Phase 4 plans will cover it:
```bash
grep -n "CONTACT-01" .planning/phases/03-agent-intelligence/03-*.md
```

This confirms no Phase 3 plan still claims CONTACT-01.
</verification>

<success_criteria>
- CONTACT-01 is formally assigned to Phase 4 in both ROADMAP.md and REQUIREMENTS.md
- Phase 3 no longer claims CONTACT-01 — the orphaned requirement gap is resolved
- The rationale is documented: Phase 4 TTS/pushInterrupt wiring is the correct implementation home
- No production code was changed — this is planning documentation only
- Phase 4 plans will pick up CONTACT-01 when the voice pipeline is built
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-intelligence/03-06-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
